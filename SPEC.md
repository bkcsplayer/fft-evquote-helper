# SPEC: 14 天无动作自动催单

## 问题（用户视角）

订单会悄无声息地卡住，没人发现。真实例子：`FFT-2026-0002`（Raju）2026-06-30 发出报价后，客户既没签也没拒，系统不提醒、没人跟进，**整整 28 天过去才在一次人工排查里被发现**。EV 单、三条新服务线、清洁订阅都有同样的黑洞。

## 方案（用户视角，一段话）

每天早上系统自己扫一遍所有未完结的订单，把"距离上一次真正推进已经超过 14 天"的挑出来：如果卡在等客户做事（签字 / 付款 / 预约），就给客户发一条短信提醒；如果卡在等我们做事（出报价 / 定价 / 申 permit），不打扰客户，而是汇总成一封邮件发给 Kuo。同一张单最多自动催 3 次（第 14 / 28 / 42 天），之后不再打扰，改为在后台标出来等人工处理。上线初期所有催单都先重定向到 Kuo 本人，确认判定逻辑和文案都对了，再放开给真实客户。

## 已拍板决策

1. **"无动作"的判定基准** → 以**最后一次状态变更**为准（EV 用 `case_status_history` 最新一条；服务单 / 清洁订阅用各自的状态变更时间）。不用 `updated_at`（后台随手编辑会重置时钟，卡住的单反而永远催不到），也不用"最后一次通知"（客户收到一封无关邮件就会被跳过）。
2. **催单次数封顶** → 停滞第 **14 / 28 / 42** 天各催一次，最多 **3 次**；之后不再自动发送，改为标记**待人工跟进**。
   - **待人工跟进的呈现方式（不做界面）**：在每天给 Kuo 的汇总邮件里单列一节列出这些单，并在该订单写一条内部备注。**不新增任何后台 UI**——为一个徽章走完整 UI 契约流程不划算，需要后台徽章时另开一次 build。
3. **双向提醒** → **球在客户**的停滞单发短信催客户；**球在我们**的停滞单不碰客户，每天汇总成一封邮件发给 Kuo（`cool@khtain.com`）。没有停滞单就不发邮件，不制造噪音。
4. **状态分流表**（本次的判定口径）：

   | 球在客户（发短信催） | 球在我们（汇总邮件给 Kuo） | 不处理 |
   |---|---|---|
   | EV `pending` 等约勘察 | EV `survey_completed` 勘察完没出报价 | EV `survey_scheduled` / `installation_scheduled`（已有预约等日子） |
   | EV `quoted` 等签字 | EV `quoting` 报价做了一半 | EV `completed` / `cancelled` / `lost`（终态） |
   | EV `permit_approved` 等约安装 | EV `customer_approved` 没去申 permit | 服务单 `scheduled` / `in_progress` / `install_scheduled` |
   | EV `installed` 等付尾款 | EV `permit_applied` permit 卡着 | 服务单 `completed` / `cancelled`（终态） |
   | 鸟网 `quoted` 等客户签字 | 鸟网 `survey_scheduled` 勘察完没录报价 | 已付款且排期正常的清洁订阅 |
   | 鸟网 `approved` 等付定金<br>（Kuo 于闸 2 补入，逻辑同 EV `installed` 等付款） | | |
   | 清洁订阅 已报价未付款 | 清洁订阅 `pending_quote` 等我们定价 | |
   | 诊断 `submitted` 等客户确认时间 | | |

5. **调度落点** → VPS **cron 每天 Calgary 时间 10:00** 打一个带共享密钥的后端端点。本栈没有 Celery/APScheduler，不为这一个功能引入调度器（YAGNI）。一天一次也天然避开了半夜发短信，不再单独做静默时段逻辑。
6. **去重与计数** → 每条催单都写进 `notifications` 表，"这单催过几次"直接从该表数，**不新建计数字段**。同一天重复调用端点不得重复发送（当日已催过则跳过）。
7. **催单重定向（红线）** → 默认打开：短信收件人一律替换成 `+15879669668`、邮件替换成 `cool@khtain.com`，正文标出本该发给谁（例如 `[→ Raju +1403…]`）。关闭方式是改环境变量，需要显式操作。生产库里只有 `FFT-2026-0002` 是真实客户，其余全是 `MOCK-`/`Mock-` 标记的演示数据。
8. **文案语言** → 纯英文，与现有通知一致（2026-07-23 已确认不做中英双语）。

## 术语引用

`停滞`、`催单`、`球在客户`、`球在我们`、`催单重定向`、`待人工跟进` —— 定义见 `CONTEXT.md`。

## Out of Scope

- **不做任何后台界面改动**——不做催单列表页、不做手动触发按钮、不做逐单开关、也不加"待人工跟进"徽章。本次是纯后端 + cron，没有 UI 契约。
- **不做客户退订 / "停止打扰"链接**。
- **不做多语言催单文案**，纯英文。
- **不改任何现有通知的触发逻辑与模板**，只新增催单自己的模板。
- **不引入 Celery / APScheduler / Redis** 或任何新的常驻进程。
- **不做催单效果统计**（打开率、转化率）。
- **不动 `FFT-2026-0002` 的任何业务数据**。

## Open risks

None —— 决策清单已由 Kuo 于 2026-07-28 逐条确认定稿。
